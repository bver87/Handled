services:
  handled:
    build: .
    container_name: handled
    restart: unless-stopped
    ports:
      - "8005:3000"
    volumes:
      - handled-data:/data
    environment:
      - TZ=Europe/Amsterdam
      - PORT=3000
      - DATA_FILE=/data/handled.json

volumes:
  handled-data:
